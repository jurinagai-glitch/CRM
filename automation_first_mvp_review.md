# Relay CRM：自動化中心・無料MVPの再評価

## 問題設定

Relay CRMの価値は、営業の記録作業を増やすことではなく、すでにある問い合わせ・議事録・提案準備を、入力し直さず次の判断とアクションへ変換することにある。現行の画面は、取引先、議事録、更新、データ品質を広く扱える一方、MVPにとっては画面数と判断面が増え、毎日の利用者が「次にどこを開くか」を考える余地を残している。

今回の再設計では、機能を減らす基準を「保存できるか」ではなく、**手入力、情報の探索、期限確認、引き継ぎのいずれかを実際に減らすか**に置く。

## 公開デザインシステムから採る原則

Atlassianは、色をアクセントではなく、主要操作、情報、成功、注意、危険などの役割で使い分ける設計を明示している。[1] GitHubのPrimerは、表の密度をCondensed、Normal、Spaciousから選べるようにし、データ量と可読性の両方を扱っている。[2]

Relay CRMでは、これを次のように適用する。

| 参考原則 | Relay CRMでの適用 |
|---|---|
| 色は意味で固定する | Relay Inkは主要操作と選択中、琥珀は確認待ち、緑は完了、赤は期限超過だけに限定する。 |
| 通常状態を彩色しない | 本文、罫線、背景はニュートラルな高コントラストへ寄せ、淡い色の上に小さい文字を置かない。 |
| 行は比較のために使う | 問い合わせ・顧客・次アクションは表の行に集約し、説明カードを常時並べない。 |
| 密度を選べるようにする | 日常のキューはCompact、原文・AI確認面はNormal、初回設定はSpaciousにする。 |
| 操作を1つに絞る | 画面上部の主要CTAは、今の業務段階に応じて「次の処理を始める」一つにする。 |

## 現行画面の課題

| 課題 | 自動化を阻害する理由 | 改善方向 |
|---|---|---|
| 画面が業務区分ごとに増えている | 利用者が判断のために複数画面を往復しやすい | ホームを自動化インボックスへ寄せ、例外だけを開く構成にする。 |
| 取引先・更新・ナレッジの情報が同時に見える | 判断材料と今すぐ行う作業の優先度が混ざる | 取引先の詳細は深掘り画面とし、日常利用は「今日の処理」に集約する。 |
| 淡い背景と小さなステータスが多い | 情報の強弱が低く、視認性が下がる | 本文コントラスト、行高、状態ラベルの面積、CTAを体系的に改める。 |
| AI以外の手動確認面が多い | 「自動化」が画面追加として知覚される | AIが処理した結果を1つの受信箱に集約し、人は例外と確定だけを扱う。 |

## 無料で進めるための結論

Supabase Freeは、500MBのデータベース、1GBのファイルストレージ、月50,000 MAU、月500,000 Edge Function呼び出しを含むため、小規模チームが正本データ、認証、権限、議事録テキストを検証するPoCには足りる。ただし、無料プロジェクトは1週間非アクティブで一時停止し、自動バックアップは含まれない。[3]

Gemini APIのFree Tierは一部モデルの入出力トークンを無料にしているが、Free Tierでは入力コンテンツが製品改善に使用される条件がある。したがって、**実在顧客の議事録を本番の無料AI処理へ送る設計は採用しない**。開発用の架空データ・匿名化済みデータに限るか、AIを切った状態でも回る入力・確認フローを正本とする。[4]

Cloudflare Workers Freeは日100,000リクエスト、D1は日500万行読み取り・10万行書き込みを含むが、これは将来の軽量な定期チェックや通知処理の選択肢である。本プロトタイプでは、まずSupabaseのDB・Authと明示実行型の処理を優先し、2つのバックエンドを同時に持ち込まない。[5]

| 自動化 | 無料MVPで採用 | 実現方法 | 理由 |
|---|---|---|---|
| 問い合わせ→取引先・初回商談 | 採用 | 会社名、担当、本文、流入経路を確定的に引き継ぐ | 再入力を減らし、AIや外部連携を必要としない。 |
| 議事録の構造化 | 条件付きで採用 | `課題`、`予算`、`決裁`、`時期`、`担当`、`期限`の記法をルールで抽出。AIは匿名テスト用の任意機能 | 機密データをFree Tierへ送らず、最小限の自動化を残す。 |
| 次アクションの作成 | 採用 | 議事録から抽出した担当・期限候補を下書きにし、人が確定 | 転記と期限見落としを減らす。 |
| 期限・更新の通知 | 採用 | DBの日付を基準にアプリ内インボックスへ表示 | メール送信・外部連携なしで、日々の確認を減らす。 |
| Gmail自動取込 | 保留 | OAuth、同期、保存・除外の監査が必要 | 実装・審査・保守負荷が無料MVPの目的に対して重い。 |
| 意味検索・類似商談 | 保留 | 埋め込み、評価データ、再生成が必要 | 最初はタグ・業種・課題による検索で十分に検証できる。 |
| 自動メール送信 | 保留 | 文面、宛先、承認、送信履歴の設計が必要 | 誤送信のコストが高く、手動送信＋下書きまでに留める。 |

## 自動化インボックスの検証

自動化インボックスから「AI下書きの確認」を選ぶと、議事録原文と確認用下書きを並べた画面へ遷移することを確認した。新しいホームは、取引先、更新、ナレッジなどの一覧を最初に見せず、ユーザーが確認する4件の処理だけを表示する。これにより、日常利用の開始点を「探索」から「例外の確認」へ変更している。

## 参考資料

[1]: https://atlassian.design/foundations/color "Atlassian Design System — Color"
[2]: https://primer.style/product/components/data-table/ "GitHub Primer — DataTable"
[3]: https://supabase.com/pricing "Supabase Pricing"
[4]: https://ai.google.dev/gemini-api/docs/pricing "Gemini API Pricing"
[5]: https://developers.cloudflare.com/workers/platform/pricing/ "Cloudflare Workers Pricing"
