# 営業事務ゼロCRM：UI・機能・実装設計レビュー

**作成日**：2026年8月13日  
**対象**：添付の「営業事務ゼロCRM 設計まとめ」および Relay CRM プロトタイプ  
**作成者**：Manus AI

## 総評

提案されているCRMの核、すなわち **「商談情報を増やすためのCRMではなく、営業事務を減らして次の行動を明確にするCRM」** という方向性は明確です。特に、議事録の原文を別サービスから貼り付け、構造化した下書きを人が確認して保存する流れは、少人数チームが最初に採用すべき現実的な自動化です。

今回のプロトタイプでは、単に画面を列挙するのではなく、営業担当者が毎日行う判断を中心に画面を組み直しました。中心となるダッシュボードでは「今日止めない商談」「AIが整理した確認用の下書き」「進行中の商談」を同時に把握できます。インバウンド、取引先・商談、議事録整理、打ち合わせ準備、ナレッジ、次アクションをサイドバーから切り替えられる、操作可能なフロントエンド・プロトタイプです。

> **結論**：設計案はMVPとして十分に成立します。ただし、本番実装では「AIの抽出結果」と「営業活動の確定データ」を混同しないこと、無料AI枠へ機密性の高い議事録をそのまま送らないこと、ブリーフィング生成を全文連結ではなく検索・根拠表示付きの仕組みにすることが重要です。

| 観点 | 評価 | 判断 |
|---|---:|---|
| 業務フローの一貫性 | A- | インバウンド→取引先→商談→議事録→次アクションの主線が明確です。 |
| データ設計の出発点 | B+ | 主要エンティティは揃っていますが、AI実行履歴・活動履歴・商談ステージ履歴を補強すべきです。 |
| AI活用の現実性 | B | 要約・抽出はMVP向きです。一方、無料枠の機密性・レート変動を前提に、確認フローと代替策が必要です。 |
| 無料枠でのPoC適性 | A- | テキスト中心・90商談/月なら有力です。添付PDFの蓄積量と無料AIへの送信データが主な制約です。 |
| 新入社員向けUX | A- | 業務手順とナレッジを商談文脈へ接続すれば、属人化の緩和に寄与します。 |

---

## 実装済みプロトタイプ

### 体験として追加した機能

プロトタイプでは、営業活動を入力画面の集合ではなく、**「今日の判断線を辿るワークベンチ」**として設計しました。ダッシュボードは期限、AI下書き、パイプラインを上下に連続させ、どの情報から確認するべきかを自然に示します。行を選択する操作、各画面への遷移、議事録の「まとめる」、抽出結果の「内容を確認して保存」、取引先の切替、ナレッジの検索・カテゴリ選択、各種の登録操作は、プロトタイプ内でインタラクションとして確認できます。

| 画面 | 実装した操作・表示 | 業務上の狙い |
|---|---|---|
| ダッシュボード | 今日の期限、未対応インバウンド、AI下書き、パイプライン、商談行を表示 | 最初の30秒で「次に何を止めないか」を判断する。 |
| インバウンド | 問い合わせの一覧、流入経路、対応状況、取引先化の導線 | 二重入力なしで初回対応から商談化へ進める。 |
| 取引先・商談 | 取引先一覧、見込み月額・確度、時系列タイムライン | 会社・商談・経緯を分断せずに把握する。 |
| 商談・議事録 | 原文貼り付け、AI整理の確認用下書き、保存操作 | 生成結果を確定情報にせず、人が確認してから保存する。 |
| 打ち合わせ準備 | 過去の文脈、今回確認すること、参照ナレッジを表示 | 会議前の情報探索を短縮し、確認項目を揃える。 |
| ナレッジ | タグ、検索、オンボーディング導線 | 個人の経験を、次の担当者が使える形に変える。 |
| 次アクション | 期限順・横断一覧、完了操作 | 商談ごとの「やるべきこと」を埋もれさせない。 |

### デザインレビュー

デザイン方針には、**Quiet Operations Desk** を採用しました。オフホワイトの紙面、Relay Ink（深い藍）の判断線、控えめな琥珀色の期限・注意表示を使い、一般的なSaaSダッシュボードの強いカード感や過剰なグラデーションを避けています。本文は Noto Sans JP、重要な見出しは Noto Serif JP とし、数字や期限は比較しやすいサイズと位置に固定しました。

AIは主役ではなく、あくまで **「保存前の下書き」** として扱っています。濃色の機能パネルではなく、左側のRelay Ink罫線、`DRAFT / 確認待ち`、確認操作を持つ用紙状の面に変更しました。これにより、ユーザーがAIの出力をそのまま事実として扱わず、編集・保存する責任を持つことを視覚的にも伝えています。

また、ヒーロー、KPI、アクション行、パイプラインに、細い罫線、点線ガイド、小さな編集注釈を反復させました。この「営業ノート上の判断線」は、実務画面としての統一感をつくるための固有モチーフです。大きな角丸と影は、主面ではなく一時的に浮く操作だけに限定しています。

| デザイン上の判断 | 実装内容 | UXへの効果 |
|---|---|---|
| 最優先情報の明示 | 期限が近いアクションを、商談より先に表示 | 画面を見た後の最初の行動が明確になります。 |
| AIの控えめな表示 | 紙面・左罫線・下書きラベル・確認操作 | AIを補助者として扱い、誤保存を抑えます。 |
| 情報密度の制御 | 一覧は行、判断は小さな状態色、説明はメタ情報に分離 | 長時間使っても視線の負荷を抑えます。 |
| 新入社員の導線 | ナレッジにオンボーディング入口を設置 | 「何を読むべきか」を探させません。 |
| モバイルの優先順位 | ハンバーガー、作成、今日の判断、次アクションを先頭に配置 | 移動中でも確認・記録の主機能に届きます。 |

### 現時点の範囲

この成果物は、デザインと操作導線を検証するための静的フロントエンド・プロトタイプです。実データベース、ログイン、ファイルアップロード、Supabase連携、Gemini API呼び出し、検索インデックス、実際のPDF解析はまだ接続していません。したがって、画面上のAI整理・保存・通知は体験を確認するための模擬動作です。

---

## データモデルのレビュー

### `meeting_summaries` と `next_actions` の重複

現在の `meeting_summaries.next_actions_raw` と `next_actions` は、役割を明確にしなければ二重管理になります。推奨するのは、**AIが返した原案を保存する領域**と、**ユーザー確認後の正式なアクションを保存する領域**を分離することです。

`next_actions` は、期限・担当・完了状態を持つ業務上の正本にします。一方で、AIの抽出結果は `ai_runs.output_json` あるいは `meeting_extractions.draft_json` に、モデル名・プロンプト版・生成時刻・入力元と共に保管します。画面で編集・承認されたタイミングで、`next_actions` を作成または更新します。この構造なら、AIの抽出品質を検証でき、誤抽出を上書きせずに追跡できます。

| テーブル／概念 | 正本として持つ内容 | 推奨する変更 |
|---|---|---|
| `meeting_notes` | 原文、実施日、取引先、商談、作成者 | `source_type`、`source_url`、`content_hash` を追加します。 |
| `meeting_summaries` | ユーザーが確認した要約・BANT等の構造化結果 | `revision`、`approved_by`、`approved_at` を追加するか、抽出テーブルから確定版へ昇格させます。 |
| `next_actions` | 実行・完了管理の正本 | `created_by`、`completed_at`、`source_type`（手入力／AI抽出）を追加します。 |
| 新設：`ai_runs` | 入出力、モデル、プロンプト版、失敗理由、トークン推定 | 監査、再実行、費用・品質管理の基礎になります。 |
| 新設：`activities` | タイムライン用の統一イベント | 議事録・提案書・ステージ変更・アクション完了を一つの時系列にできます。 |
| 新設：`deal_stage_history` | ステージ変更の履歴 | 滞留日数、失注要因、パイプライン改善を後から分析できます。 |

### BANT項目の構造化は妥当か

`issue`、`budget`、`decision_maker`、`timeline` を構造化して持つ方針は妥当です。ただし、これらを一つの要約だけに閉じ込めると、最新の状況・根拠・未確認を区別できません。営業では、予算は「未確認」から「月額50万円前後」へ変化し、決裁者も複数になることがあります。

そのためMVPでは `deal_qualification` を商談に1対1で設け、各項目に `value`、`status`（未確認／仮説／確認済み）、`source_meeting_note_id`、`confirmed_at` を持たせる方法が扱いやすいです。決裁者は文字列ではなく `contacts` と多対多でつなぐか、少なくとも `decision_makers jsonb` として複数人・役割・影響力を表せるようにします。

### 追加すべき制約

取引先化では `converted_company_id` だけでなく、`converted_deal_id` と `converted_at` を持たせると追跡しやすくなります。`deals.amount` は月額・年額・一時金が混ざると集計が壊れるため、`amount_value`、`currency`、`amount_type`（MRR／ARR／one_off）を分けてください。また、提案書は `proposals` にURLだけを持つより、ファイル名、MIMEタイプ、サイズ、版数、作成者、削除状態を持つ `attachments` を汎用化し、`proposals` は商談上の意味を表すテーブルにする方が拡張に強くなります。

---

## AI機能の実装レビュー

### 議事録整理は「同期・明示実行・承認保存」から始める

最初の実装では、原文を貼り付けた後にユーザーが明示的に「まとめる」を押す同期処理が適切です。自動実行やバックグラウンド処理を初期から増やす必要はありません。API呼び出しには、入力原文のハッシュとプロンプト版を含め、同じ議事録に対する重複生成を防ぎます。レート制限時は指数バックオフを行い、ユーザーには「処理中」「再試行可能」「保存前の下書き」の状態を正確に表示します。

AIの出力は自由文ではなく、JSON Schemaで `summary`、`decisions[]`、`next_actions[]`、`qualification`、`unknowns[]`、`evidence[]` を返す設計を勧めます。`evidence[]` に原文の引用箇所または段落番号を含めれば、営業担当者は抽出根拠を確認できます。特に予算・決裁・導入時期は、AIが推定した場合に **「未確認」** を返せるようにし、空欄をもっともらしい内容で埋めないルールが必要です。

### ブリーフィング生成はRAGと根拠表示が必要

「過去議事録・提案内容・ナレッジをすべてLLMへ渡す」方式は、商談の蓄積に伴って遅く、高コストで、古い情報や無関係な内容を混ぜやすくなります。取引先ID・商談ID・更新日・タグを使った絞り込みを先に行い、その後でベクトル検索または全文検索により関連文書を上位数件だけ選択してください。

生成結果には、参照した議事録・提案書・ナレッジのタイトルと日付を表示します。さらに「前回の決定事項」「今回確認すること」「未確認のBANT項目」「使えるナレッジ」を別セクションにし、情報の出所をクリックして戻れるUIが必要です。これにより、ブリーフィングがブラックボックスの文章ではなく、会議準備のための検証可能な編集物になります。

### ナレッジは自動公開しない

ナレッジの抽出候補をAIが作ること自体は有効です。ただし、顧客固有の前提を一般論として登録する、古い成功体験が残る、個人情報を含む、といった問題が起きます。`draft → review → published → archived` の状態、オーナー、最終見直し日、参照元を持たせ、AIの候補は必ずレビュー待ちとして登録してください。

---

## 無料構成の実現性と選択肢

SupabaseのFreeプランは、現時点でプロジェクトあたり500MBのデータベース、1GBのファイルストレージ、5GBのegress、50,000 MAUを含み、2つのアクティブな無料プロジェクトを持てます。一方で、無料プロジェクトは1週間の非活動で一時停止し、自動バックアップは含まれません。[1] [2]  

90商談/月、テキスト議事録中心、少人数利用という前提なら、**DB容量はPoCに対して十分な可能性が高い**です。たとえば議事録の生テキストが1件あたり数KB〜数十KBであれば、90件/月でもテキスト本文そのものは500MBに対して小さい規模です。ただし、添付PDFを保存し始めると1GBのストレージの方が先に制約になりやすいため、ファイルサイズ上限、外部リンクの許容、世代保持数、削除ルールを初期に決めるべきです。[1]

Gemini APIのFree Tierは対象モデルの入出力トークンが無償ですが、送信コンテンツはGoogleの製品改善に利用される条件です。無料枠のレート制限はプロジェクト・モデルごとに変動し、保証されません。Google AI Studioで実際の上限を監視する設計が必要です。[3] [4] したがって、社外秘の議事録を扱う本番CRMとしては、無料枠を「検証環境」または「匿名化済みデータ用」に限定する判断が安全です。

| アプローチ | 内容 | トレードオフ | 費用 | 導入難易度 |
|---|---|---|---|---|
| **小規模PoC** | Supabase Freeと、対象モデルのGemini Free Tierで、手動実行の議事録整理だけを提供する | 最小コストで検証できますが、無料AIのデータ利用条件と変動するレート制限を受け入れる必要があります。 | 原則$0の範囲で開始可能 | 低 |
| **機密データ対応のMVP** | Supabaseを継続利用し、AIは課金設定済みの提供形態へ移し、月次上限・監査ログ・RLSを設定する | 初期費用と運用監視は増えますが、機密議事録を扱うためのコントロールが現実的になります。Paid Tierでは、プロンプトと応答を製品改善に利用しない条件が示されています。 | 利用量に応じる。Geminiの有料化は最低$10の事前入金が必要となる場合があります。[3] [5] | 中 |

この二つは段階的な選択肢です。最初の1〜2チームで、匿名化または同意済みのデータを使って抽出精度と業務時間削減を測るなら前者が適します。顧客の会話内容や提案内容を日常的に保存・生成する運用へ進むなら、後者のようにデータ利用条件、予算上限、監査可能性を揃えてから移行してください。

---

## セキュリティ・運用で外せない事項

営業議事録には顧客名、担当者名、予算、意思決定の情報が含まれます。DBはテーブルごとではなく、組織・チーム・所有者の単位で行レベルセキュリティ（RLS）を設計し、原文・要約・添付・AI実行履歴すべてに同じアクセス境界を適用します。ブラウザにAIプロバイダの鍵を置かず、API呼び出しは必ずサーバー側から行います。

さらに、削除・閲覧・共有の各操作を活動履歴へ記録し、議事録の保持期間を設定してください。FreeプランのSupabaseでは自動バックアップが含まれず、ログ保持も1日であるため、障害対応・誤更新対策を無料枠だけに依存しない運用が必要です。[1] これは法律上の助言ではありませんが、顧客データの社内ルール、取引先との契約、委託先へのデータ送信可否は、本番投入前に必ず確認すべき事項です。

---

## 推奨する段階的な実装順

### 第1段階：業務の正本をつくる

最初に、認証、RLS、インバウンド、取引先、担当者、商談、次アクション、活動履歴を実装します。ここではAIを入れず、「誰が・いつ・何を次に行うか」を確実に記録・表示できる状態を合格条件にします。インバウンドから取引先・商談への変換は、データをコピーせず変換履歴を残す形にします。

### 第2段階：議事録の確認用下書き

次に、貼り付けた議事録をJSON Schemaで整理し、ユーザーが編集・承認して保存する機能を加えます。`ai_runs`、冪等性キー、モデル・プロンプト版、エラー状態、根拠表示をこの段階で入れます。精度のKPIは「保存後に手修正された割合」「次アクションの期限・担当が確定した割合」「1件あたりの整理時間」です。

### 第3段階：検索可能なナレッジ

公開承認フローを持つナレッジ、タグ、全文検索を追加します。ナレッジは「業種」「商談フェーズ」「課題」「用途」「最終見直し日」で絞り込めるようにし、新入社員にはフェーズ別の必読セットを提示します。

### 第4段階：根拠付きブリーフィング

最後に、商談・議事録・ナレッジ・提案書から関連性の高いものだけを取り出す検索層を作り、ブリーフィングを生成します。結果には出典、生成日時、更新ボタン、手動編集、共有範囲を持たせます。この段階で、利用量ログによりAI費用とレート制限を可視化します。

| 合格条件 | 第1段階 | 第2段階 | 第3段階 | 第4段階 |
|---|---:|---:|---:|---:|
| 取引先・商談・アクションの登録と権限が機能する | 必須 | 継続 | 継続 | 継続 |
| 議事録から構造化下書きを作り、確認後に保存できる | — | 必須 | 継続 | 継続 |
| ナレッジを承認・タグ付け・検索できる | — | — | 必須 | 継続 |
| 出典付きブリーフィングを生成し、更新履歴を残せる | — | — | — | 必須 |

## 次に確認したい要件

実装開始前に、まず **利用者の組織境界**、**議事録に含まれる機密情報の扱い**、**提案書をファイル保存するのか外部リンク中心にするのか** を確定してください。これらはRLS、AIの利用形態、ストレージ設計を大きく左右します。次に、商談金額が月額・年額・一時金のどれを指すのか、失注理由をどこまで分類するのか、既存顧客と見込み顧客を同じ取引先として扱うのかを定義すると、ダッシュボードの数字も安定します。

## 参考資料

[1]: https://supabase.com/pricing "Supabase Pricing"
[2]: https://supabase.com/docs/guides/platform/billing-on-supabase "Supabase Billing Documentation"
[3]: https://ai.google.dev/gemini-api/docs/pricing "Gemini API Pricing"
[4]: https://ai.google.dev/gemini-api/docs/rate-limits "Gemini API Rate Limits"
[5]: https://ai.google.dev/gemini-api/docs/billing "Gemini API Billing"
