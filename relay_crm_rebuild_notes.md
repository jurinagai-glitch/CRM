# Relay CRM：大規模業務プロダクトへの再設計メモ

## 診断の前提

Claudeの参考モックアップURLは閲覧を試みたが、アクセス時点では公開ページとして取得できなかった。そのため、添付された設計方針と、Relay CRMの実画面を正本として再設計する。参考モックアップの見た目を推測して再現することはしない。

## Claude方針への評価

「ライトテーマ」「色数を抑える」「AIを下書きとして扱う」「余白とコンポーネント値を統一する」という原則は妥当である。一方で、Linear・Notion・Attio・SmartHR・freeeを一つの視覚トーンとして混ぜたり、英字フォントをInterへ統一したりするだけでは、固有の業務体験にはならない。

再設計では、参考サービスを見た目のテンプレートとして使わず、次の業務原則に置き換える。

| 原則 | 実装上の意味 |
|---|---|
| 判断を先に置く | 画面上部に「今日決めること」「期限切れ」「確認待ち」を置き、全件データは下に置く。 |
| 記録と確定を分ける | 原文、AI下書き、ユーザー確認済みの情報を、色ではなく構造とラベルで区別する。 |
| 顧客文脈を切らない | 取引先、商談、提案、契約、活動を横断して遷移できる。 |
| 表は作業面、カードは例外面 | 日常の比較・検索・更新には密度の高い表を使い、カードは要確認事項とガイドに限定する。 |
| 体系で整える | 余白、罫線、状態、操作の強度を、全画面で同じトークンとコンポーネントに揃える。 |

## MVPで維持する業務導線

1. インバウンドを登録または取り込み候補として確認する。
2. 重複候補を確認して、取引先・担当者・商談へ変換する。
3. 議事録原文を貼り付けて、AI下書きを生成する。
4. 根拠を確認し、次アクションと商談情報を確定する。
5. 取引先の活動履歴、提案、契約更新を一つの顧客文脈で追う。
6. 打ち合わせ前に、出典付きのブリーフィングと類似事例を参照する。

## 今回の再設計範囲

既存のライトテーマと「AIは下書き」方針を維持する。一方、装飾的なヒーロー、画面ごとに異なるカード構成、過剰な紙面モチーフは抑え、企業向け業務SaaSに必要な表形式、コンテキストバー、状態フィルタ、密度制御、例外処理の見え方へ整理する。

## 実画面での検証結果

デスクトップでは、インバウンド、取引先、議事録、次アクション、契約更新の各画面を、作業リスト・現在の判断面・判断コンテキストの三層構成へ揃えた。特に、インバウンドの「取引先と商談を作成」操作は、取引先ワークスペースへ遷移し、問い合わせ原文・受信経路・担当を引き継ぐ旨の完了通知を返すことを確認した。

議事録では、原文を表示した状態から「下書きをつくる」を実行すると、AIの整理結果が確認待ちの下書きとして現れることを確認した。下書きには、商談サマリ、決定・確認事項、担当・期限を含む次アクション候補、未確認の重要事項、原文根拠への導線を含め、保存・反映前に人が内容を吟味できるようにした。

下書きの「確認して商談へ反映」を実行すると、完了通知とともに次アクション画面へ遷移することを確認した。次アクション画面では、取引先、作業内容、根拠、担当、期限、優先度を同じ行で確認でき、根拠を議事録・商談へ戻って確認できる構成とした。

モバイルでは、三層を一列に積み替える。リストが先、現在の判断面が次、根拠・確認事項が最後という順番を維持し、横幅不足によって文脈が欠けないことを確認した。

## 残すべきMVP機能と後回しにする機能

| 優先度 | 残す・着手する機能 | 後回しにする機能 |
|---:|---|---|
| P0 | インバウンド取引先化、取引先ハブ、議事録原文、AI下書き確認、次アクション、活動履歴、組織/RLS | Gmail自動取込、メール本文の保存、類似商談の自動推薦 |
| P1 | 提案資料メタデータ、契約更新、重複候補の確認、ナレッジ承認 | ベクトル検索、詳細レポート、複雑な自動化ルール |
| P2 | 出典付きブリーフィング、キーワード＋メタデータ検索 | Gmail Push同期、埋め込みの非同期運用、予測モデル |
