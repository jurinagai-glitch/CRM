# Relay CRM：Claude編集ガイド

## 目的

このコードベースは、営業事務を減らすための **自動化中心・無料MVP** のフロントエンドプロトタイプです。見た目を増やすことより、ユーザーが毎朝「自動化インボックス」で確認すべき項目だけを見て、問い合わせ・議事録・次アクションを短い手順で終えられることを優先してください。

## 実行方法

```bash
pnpm install
pnpm dev
pnpm check
pnpm build
```

| 場所 | 役割 |
|---|---|
| `client/src/pages/Home.tsx` | アプリシェル、ナビゲーション、画面遷移。 |
| `client/src/components/AutomationHub.tsx` | デフォルト画面。確認が必要な自動化処理を集める。 |
| `client/src/components/InboundTriage.tsx` | 問い合わせから取引先・初回商談への変換。 |
| `client/src/components/AccountsWorkspace.tsx` | 取引先・商談・根拠・次回会議の作業面。 |
| `client/src/components/MeetingWorkbench.tsx` | 議事録原文と確認用下書き。 |
| `client/src/components/ActionRunway.tsx` | 次アクションの実行・完了。 |
| `client/src/components/Renewals.tsx` | 契約更新とデータ品質の補助画面。 |
| `client/src/automation-first.css` | 現行の高コントラスト・業務UIテーマ。 |
| `client/src/enterprise-refinement.css` | 顧客・議事録・実行の三領域ワークスペース。 |
| `client/src/index.css` | Tailwindテーマと各追加スタイルの読み込み。 |

## 絶対に守る制約

| 領域 | 制約 |
|---|---|
| MVPの中心 | 「自動化インボックス」から開始し、確認が必要な処理だけを優先表示する。 |
| 自動化 | 問い合わせの項目引継ぎ、ルールベースの議事録整理、確定後のアクション作成、期限・更新のアプリ内表示に限定する。 |
| AI | 顧客データを無料の外部AIに送る前提にしない。画面上では「自動整理の下書き」と表現し、人の確認前に確定情報へ反映しない。 |
| 保留範囲 | Gmail自動取込・自動送信・意味検索・複雑なレポートを、設計なしに追加しない。 |
| デザイン | 主操作・選択＝青、完了・安定＝緑、確認待ち＝琥珀、期限注意＝赤。装飾のための色やグラデーションは使わない。 |
| レイアウト | 主要画面は「作業リスト → 現在の判断面 → 判断コンテキスト」を基本にする。カードを増やしすぎず、一覧は行・表・罫線で構成する。 |
| アクセシビリティ | 状態を色だけで伝えず、ラベルと文字を伴わせる。小さな補助文字を主情報に使わない。 |

## いまの実装範囲

画面と遷移は実装済みですが、DB・認証・保存・AI API・メール連携は未実装です。データはコンポーネント内のサンプル配列であり、`toast` とローカル状態で操作を模擬しています。

本番への次の実装対象は、Supabaseの認証とRLS、`companies`、`contacts`、`inbound_inquiries`、`deals`、`meeting_notes`、`extraction_drafts`、`next_actions`、`activities`の正本データです。下書きは正式データと分離し、人が承認した時だけアクションや商談へ反映してください。

## Claudeへの編集依頼例

```text
このRelay CRMのReact/TypeScriptプロジェクトを編集してください。

目的は、営業事務を減らす無料MVPです。自動化インボックスを日常の起点とし、ユーザーには確認・確定が必要な処理だけを表示してください。

今回変更したいこと：
1. [変更したい具体的な業務フロー]
2. [追加または削除したい画面・情報]
3. [成功条件]

制約：
- Gmail同期、メール自動送信、意味検索、外部AIへの顧客データ送信は追加しない。
- 既存の青・緑・琥珀・赤の状態色と、高コントラストの表中心UIを守る。
- 議事録の下書きは人が確認するまで正本データへ反映しない。
- `pnpm check` と `pnpm build` が通る状態で返す。

変更したファイル、変更理由、動作確認の手順、未対応の制約を最後に一覧化してください。
```

## レビュー資料

詳細な判断材料は、次の資料を参照してください。

| 資料 | 内容 |
|---|---|
| `claude_automation_first_review.md` | 自動化中心・無料MVPを他AIにレビューしてもらうための要件と質問。 |
| `automation_first_mvp_review.md` | 無料枠、機能の採否、デザイン判断の根拠。 |
| `ideas.md` | 現行のデザイン原則と変更してはいけないスタイル判断。 |
